version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      # - echo "Installing dependencies..."
      # - pip install requests -t lib
  build:
    commands:
      - echo "Zipping deployment package..."
      - ls
      # - cd backend
      # - echo "Inside lib"
      # - ls
      # - zip -r9 ../deployment_package_1.zip .
      # - cd ..
      - zip -r -j deployment_package_1.zip backend
      - cd ..
  post_build:
    commands:
      - echo "Updating lambda Function..."
      - aws lambda update-function-code --function-name login-backend --zip-file fileb://deployment_package_1.zip
      - echo "DONE!!"